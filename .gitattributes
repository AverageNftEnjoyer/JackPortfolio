# Auto-detect text files and normalize line endings
* text=auto

# Ensure all HTML, CSS, JavaScript, JSON, and other web files are treated as text
*.html text
*.css text
*.js text
*.json text
*.xml text
*.svg text
*.md text

# Treat image files as binary (no line-ending normalization)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary

# Treat font files as binary
*.ttf binary
*.woff binary
*.woff2 binary

# Treat video and audio files as binary
*.mp4 binary
*.mp3 binary
*.wav binary
*.ogg binary

# Treat other binary files like PDFs
*.pdf binary

# If you're using Git LFS (Large File Storage) for media assets, you might add:
# *.png filter=lfs diff=lfs merge=lfs -text
# *.jpg filter=lfs diff=lfs merge=lfs -text
# *.mp4 filter=lfs diff=lfs merge=lfs -text
